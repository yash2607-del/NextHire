# Production backend base URL used at build time by Vite.
# You can also override this in Vercel Project Settings -> Environment Variables.
#
# Note: including `/api` here is supported; the client will not double-append it.
VITE_API_URL=https://mynexthire.onrender.com
